{
  "name": "dude",
  "private": true,
  "description": "Dude — a desktop distribution of the DeepSeek Harness: the official dsh core and web UI plugins at locked versions, fronted by Dude's own UI plugin and shipped in an Electron shell.",
  "bin": {
    "dude": "scripts/dude"
  },
  "scripts": {
    "build": "pnpm -r --filter './plugins/*' build",
    "test": "pnpm -r --filter './plugins/*' test",
    "typecheck": "pnpm -r --filter './plugins/*' typecheck"
  },
  "devDependencies": {
    "@deepseek-ai/dsh": "0.1.5-rc.3",
    "esbuild": "^0.28.2"
  },
  "engines": {
    "node": "^22.19.0 || >=24.0.0"
  }
}
