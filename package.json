{
  "name": "deepbuddy",
  "private": true,
  "description": "DeepBuddy — a desktop distribution of the DeepSeek Harness: the official dsh core and web UI plugins at locked versions, fronted by DeepBuddy's own UI plugin and shipped in an Electron shell.",
  "bin": {
    "deepbuddy": "scripts/deepbuddy"
  },
  "scripts": {
    "build": "pnpm -r --filter './plugins/*' build",
    "test": "pnpm -r --filter './plugins/*' test",
    "typecheck": "pnpm -r --filter './plugins/*' typecheck"
  },
  "devDependencies": {
    "@deepseek-ai/dsh": "0.1.2-rc.1",
    "esbuild": "^0.28.2"
  },
  "engines": {
    "node": "^22.19.0 || >=24.0.0"
  }
}
