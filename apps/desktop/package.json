{
  "name": "deepbuddy-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Electron shell for the DeepBuddy harness UI: a hiddenInset macOS window over a local `dsh web` serving the deepbuddy plugin.",
  "main": "main.js",
  "scripts": {
    "start": "electron ."
  },
  "devDependencies": {
    "electron": "^38.2.0"
  }
}
