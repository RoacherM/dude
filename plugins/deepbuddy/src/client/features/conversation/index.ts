/**
 * The conversation feature: the sidebar's new-task entry. The main-column
 * chat render and its live state are owned by the official ui-conversation
 * `ConversationRoot`; DeepBuddy contributes only the start-session action.
 */
import { ChatNav, CONVERSATION_APP_ID } from './Chat.tsx'

export { ChatNav, CONVERSATION_APP_ID }
