# 停靠栏拖拽上限从 70% 收紧为窗口 1/3

- 日期 / agent：2026-08-26 / claude
- 我原来的理解：右列拖拽范围沿用 Synara handoff 的 30%–70%（DESIGN_INTENT §2 旧值），
  拖到近半个窗口是允许的。
- 用户实际要的：右列「原则上不能超过窗口的 1/3」——它是检查器不是并列工作区，
  可拖动范围应该很窄（30% ~ 1/3）。
- 分歧根源：范围假设——沿用了旧设计稿数值，没随浮岛 v2 重新校准右列的角色定位。
- 以后如何避免：右列宽度相关的任何钳制/默认值改动，先按「dock ≤ 窗口 1/3、
  主列保留 460、416px 下限优先」三条对一遍再动手；70% 这个旧上限不要再回潮。
  规格已落 DESIGN_INTENT §2，原型 clampDock 已实装。
