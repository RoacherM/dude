# 交互原型的当前版本是 v2 打包单文件，不是 handoff 交接包里的 v1 源码

- 日期 / agent：2026-08-17 / claude
- 我原来的理解：`design/design_handoff_atlas_harness/Desktop Harness.dc.html`（交接包源码，
  1222 行，可直接编辑）是当前设计版本，直接在上面改。
- 用户实际要的：当前版本是 `Atlas Harness 交互 Demo v2.html`（打包单文件，
  v2 视觉：圆角、1px 分隔线、字重 600、更柔的配色）。v1 交接包是它的上一代。
- 分歧根源：格式假设——我挑了「能直接编辑的那个」而不是「用户在用的那个」，
  两者恰好是不同世代。
- 以后如何避免：动原型/模板前先问或先比对哪个是现役版本（看修改时间、版本号后缀、
  用户给的路径），可编辑性不是选择依据。现役原型现已作为**设计锚点**
  收进 `deepbuddy/design/prototype/`（README 说明改法：走 `patch-prototype.mjs`，
  每条替换必须命中且仅命中一次）。

> **后记（2026-08-20）**：本文末句的「设计锚点」说法已失效。
> **早期原型（v2 / v3）已全部归档，没有任何现行效力**——当前设计只有
> `ARCHITECTURE.md` + `design/minimal-icons.html` 两份。不必再去比对哪版原型是现役。
> **本条纠偏的教训（先确认哪个是现役，不按可编辑性挑）依然成立**，
> 08-20 漏改 `design/` 正是同一个坑又踩了一遍。
