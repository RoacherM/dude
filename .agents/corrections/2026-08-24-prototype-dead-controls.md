# 原型中呈现控件不等于完成交互
- 日期 / agent：2026-08-24 / codex
- 我原来的理解：完成主演示链路和主要拖拽、tab、输入行为后，就可以把原型交付为“可交互”。
- 用户实际要的：界面上每一个可点控件都必须按真实实现与任务规格响应；不能实现的控件必须明确显示为 disabled，不能存在静默死点。
- 分歧根源：验收范围假设——把局部链路验证误当成了逐控件全量验收。
- 以后如何避免：交互原型交付前按 hero、侧栏、会话、dock、弹层逐状态枚举所有可见控件，逐一验证点击后的状态变化、disabled 视觉、cursor 和 hover。
